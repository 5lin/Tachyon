# Tachyon Backend Configuration
# Copy this file to .env and modify as needed

# ===================
# Required Settings
# ===================

# Path to your comics directory
COMICS_DIR=/opt/comics

# Server port (default: 3001)
PORT=3001

# Application URL (where to redirect after login)
# For local development with frontend: http://localhost:5173
# For production: https://your-domain.com
APP_URL=http://localhost:5173

# ===================
# OIDC Authentication (Optional)
# ===================
# Leave OIDC_CLIENT_ID empty to disable authentication (public access)

# OIDC Provider URL (e.g., https://auth.example.com or https://accounts.google.com)
# OIDC_ISSUER=https://auth.example.com

# Client ID from your OIDC provider
# OIDC_CLIENT_ID=your-client-id

# Client Secret from your OIDC provider
# OIDC_CLIENT_SECRET=your-client-secret

# Callback URL (must match the one registered with your OIDC provider)
# OIDC_REDIRECT_URI=https://your-app.com/callback
# Create endpoints manually if your provider uses non-standard paths
OIDC_AUTH_URL=
OIDC_TOKEN_URL=
OIDC_USERINFO_URL=

# Scopes to request (default: openid profile email)
# OIDC_SCOPE=openid profile email

# ===================
# Security
# ===================

# Secret key for signing sessions and URLs (use a random 32+ character string)
# If not set, a random key is generated at startup (sessions won't persist across restarts)
# SECRET_KEY=your-random-secret-key-at-least-32-chars

# ===================
# Frontend Settings (for .env in frontend folder)
# ===================
# VITE_API_URL=http://localhost:3001
